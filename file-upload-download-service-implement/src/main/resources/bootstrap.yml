#application
spring: 
  application:
    name: file-upload-download-service
  profiles:
    active: local
  cloud:
    config:
      name: zwl-base, file-upload-download-service
      discovery:
        enabled: true
        service-id: config-server

#server setting
server: 
  port: 10001
management: 
  port: 20001
